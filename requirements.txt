# Core framework
Flask==3.0.3
gunicorn==23.0.0

# Firebase / Pyrebase4
pyrebase4==4.8.0
requests==2.32.3
requests-toolbelt==0.10.1
urllib3==1.26.20   # keep below v2 to avoid old pyrebase issues

# HTML parsing / scraping (if your app uses them)
beautifulsoup4==4.12.3
lxml==5.2.2

# Environment management
python-dotenv==1.0.1

# Optional utils (add/remove as per your app code)
pytz==2025.1
